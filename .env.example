# Endpoint that receives enquiry form submissions (POST, JSON body).
# Until this is set, submissions are NOT delivered anywhere — the form will
# still render its success state but a warning is logged to the console.
VITE_ENQUIRY_ENDPOINT=
